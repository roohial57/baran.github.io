# سایت محلی پورتفolio

این پوشه یک سایت معمولی است (HTML / CSS / JS).
به GitHub یا npm وصل نیست.

## اجرا در VS Code

1. فقط پوشه `website` را Open کن
2. Live Server را نصب کن (اگر خواست)
3. روی `index.html` راست‌کلیک → **Open with Live Server**

یا مستقیم `index.html` را با Chrome باز کن.
